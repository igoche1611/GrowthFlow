# GrowthFlow Customer Retention Strategy Optimization

This package contains the completed analytical notebook and the real Streamlit dashboard deployment files.

## Run locally

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Deploy

Upload all files in this folder to a GitHub repository, then create an app in Streamlit Community Cloud using `streamlit_app.py` as the entry point.
